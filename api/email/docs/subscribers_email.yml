description: Send the authors subscribers email.
tags:
  - Email
consumes:
  - "application/json"
produces:
  - "application/json"
parameters:
  - in: query
    description: The query should contain the author id
    required: true
    name: 'id'
    type: 'string'
responses:
  200:
    description: When the confirmation email is successffuly sent

  400:
    description: Fails to update user due to bad request data
